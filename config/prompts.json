[
    "Generate a conventional commit message for this git diff. Return ONLY the commit message, No Markdown. Format: type(scope): description",
    "Create a conventional commit message. Return only the message in format: type(scope): description. No other text or formatting.",
    "Generate a git commit message following conventional commits format. Return the message only without Markdown, nothing else."
]